import 'package:flutter/material.dart';

class Tela4 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Educação'),
      ),
      body: Center(
        child: Text('Bem-vindo à Educação!'),
      ),
    );
  }
}